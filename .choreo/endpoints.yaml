version: 0.1

# +required List of endpoints to create
endpoints:
  - name: Private Bal service
    port: 8080
    type: TCP
    networkVisibility: Public
    context: /
  - name: Private DB service
    port: 8081
    type: TCP
    networkVisibility: Public
    context: /
